.cout-cash {
	margin: 0 auto;

	.cout-cash-btn {
		display: flex;
		gap: 0 8px;
		padding: 6.5px 12px;
		background-color: #ffffff;
		cursor: pointer;

		.cout-cash-text {
			font-family: 'Istok', sans-serif;
			font-size: 14px;
		}
	}
}
